# 13H Legal War Room

Repo-ready assembly of the litigation data pipeline and read-only Astro UI.

## Included
- P1 contract layer
- P4 approved linked canonical JSON split into `/src/data/*.json`
- P5 data layer
- P6 read-only UI layer

## Commands
- `npm install`
- `npm run build`
- `npm run check:ts`
- `npm run build:indexes`
- `npm run check:ids`
